name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@master

      - run: corepack enable
      - run: pnpm install --frozen-lockfile

      - name: Build 🏗️
        run: pnpm build
